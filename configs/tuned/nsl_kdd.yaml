# GENERATED record (do not hand-edit). Regenerate with: python export_configs.py
# Source: gons_configs.py -> make_gons_tuned_cfg (per-dataset TUNED grid cell).
#
# The per-dataset-TUNED comparison arm for nsl_kdd, at seed 42. Reproduce with:
#   python run_gons_main.py --arm tuned --datasets nsl_kdd
#
# This is NOT the ablation base -- the shipped ablation is on the FIXED config.
# `map_gamma` here is the 'median' bandwidth rule (the tuned arm selects the
# rule as well as the grid cell), resolved on this dataset+seed's base split.
task: fscil
experiment_name: tuned_nsl_kdd_s42
method: gons
dataset_config_path: configs/data/dataset.yaml
train_path: data/processed/nsl_kdd_ta/train.csv
calibration_path: data/processed/nsl_kdd_ta/calibration.csv
test_path: data/processed/nsl_kdd_ta/test.csv
output_root: artifacts/runs
base_class_ratio: 0.5
base_class_seed: 42
novel_class_order: sorted
novel_support_fraction: 1.0
novel_support_max_k: 5
refresh_after_session: true
model:
  label_column: label
  seed: 42
  map_kind: rff
  map_kernel: rbf
  map_gamma: 0.0009095275063277201
  map_n_components: 512
  d_proj_max: 32
  residual_handling: drop
  energy_temperature: 1.0
  enable_contrastive_rff: false
  score_support_penalty_alpha: 0.0
  score_support_penalty: false
  score_support_penalty_fn: log
  enable_ncdr_classification: true
  ncdr_threshold: 0.85
  threshold_quantile: 0.85
  scoring_mode: min_distance
  refresh_mode: single_pass
  enable_local_affine_screen: false
  support_trim_strategy: uniform
  preprocessing:
    numeric_scaler: quantile
