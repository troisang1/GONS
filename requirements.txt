# GONS release — non-deep, CPU-only dependencies.
# (Prefer `pip install -e .` / `uv pip install -e .`, which uses pyproject.toml.)
numpy>=1.26,<3
pandas>=2.2,<3
scikit-learn>=1.5,<2
scipy>=1.13,<2
pydantic>=2.8,<3
pyyaml>=6.0,<7
rich>=13.7,<14
